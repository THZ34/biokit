CHANGES
=======

* Change
* 新增: 共定位数据整理和共定位作图
* 修改: 修复metascape\_dotplot图例大小与数值不匹配的问题
* Change
* Change
* Change
* 新增: 批量修改conda环境中的matplotlibrc, 设置字体库路径
* Change
* 删除
* 修改: 将biokit-cli加入环境变量
* 删除
* 新增: biokit命令行脚本
* Change
* 修改: 修改默认参数
* Change
* Change
* 新增: 使用plantir进行拟时序分析的pipeline
* 修改：新增ci\_show参数用于控制是否展示95ci置信区间
* 修改：新增vmax,vmin参数用于控制颜色
* Change
* Change
* Change
* Change
* 新增：text\_similarity，匹配两个列表中相似度最高的文本
* Change
* Change
* 修改：新增注释比例
* 修改：新增license路径参数
* Change
* Change
* Change
* Change
* 新增：新增grid\_average函数，用于对二维空间进行均匀分块并计算平均数
* Change
* 修改: 通过判断箭头颜色来确定其中的字的颜色
* 修改: 增加粒细胞marker
* 修改: 雷达图增加分组选项, 可以自行绘制分组雷达图
* 修改: get\_gsm\_sampleinfo返回的df中会包含gsmid
* 修改: 优化metascape\_dotplot
* 修改: 颜色全部改成hex格式, 新增两组颜色(biomega1, biomega2)
* Change
* 新增: 新增binned\_value函数用于将值分类到指定区间
* Change
* 修改: 新增get\_text\_color用于判断箭头中文本的颜色
* 新增: 新增get\_rank\_df用于获取adata的rank\_df
* 修改: 重写metascape\_dotplot
* 修改: 修改箭头上的标注，去掉high-exp in
* Changes
* Changes
* 新增：等高线图
* 新增：雷达图
* 新增：parallel\_categories画图
* 新增：载入metascape结果
* 修改: 修复kaplan-meier计算P值的问题
* 修改: 如果只有1行1列, 返回ax而不是axes
* 修改: 修改文件的编码, 添加colorbar
* 新增: 生成互补色
* 移动：p2text\_func移动到data\_convert下
* 新增：bezier S形曲线
* 移动：cumulative\_bar移动到baseplot下
* 移动：cumulative\_bar移动到baseplot下
* Change
* 修改：增加textadjust参数用于控制是否使用adjust\_text自动调节文本间隔
* 修改：简化读写
* 新增：增加gseapy\_dotplot
* 新增：增加pathway\_circos
* 修改: 添加violin
* 修改: 改成utf-8编码
* 修改: testbox新增violin
* Change
* 新增：检查本地文件，避免重复下载
* Change
* Change
* 新增：根据ax大小以及其他参数计算并生成fig
* Change
* 新增：神经网络图
* 修改: fit\_model分为regression和classifier, 分别使用不同的方法评估性能
* 修改: feature\_ranking的在将model传入rfe时使用copy避免对原对象产生影响
* 修改: feature\_ranking的models直接传入模型而不是模型的类
* 修改：优化get\_gsm\_sampleinfo, 自动拆分characteristics
* 新增：获取kegg通路中的基因
* 修改：circos.plot\_base提取fontproperties['rotation']用于旋转文本
* Change
* Change
* 修改：新增参数legend\_bbox\_to\_anchor用于控制图例位置 修改：AUC值注释合并到图例中
* Change
* 新增：下载KEGG通路基因的命令模板
* 修改：修改脚本的编码，避免中文乱码 新增：爬取完成后复查补漏
* Change
* Change
* 新增：基于docker的msbio(metascape镜像)执行通路富集
* 修改：testbox新增interval参数用于控制box间隔 修改：testbox新增hide\_flier参数用于隐藏fliers
* Change
* Change
* 新增: 基于metascape docker镜像执行本地metascape分析
* 新增: GEO数据下载和样本信息爬取
* Change
* 新增:基因注释识别
* 修改：优化画图参数和画图风格
* 修改：优化画图参数和画图风格
* 新增：从biopython.Plyon中复制的draw\_tree函数
* 更新celltree
* 添加环境初始化脚本 添加单细胞注释数据库
* Change
* 渲染legend，否则在legend.get\_window\_extent()时会报错AttributeError: 'NoneType' object has no attribute 'points\_to\_pixels'
* 渲染legend，否则在legend.get\_window\_extent()时会报错AttributeError: 'NoneType' object has no attribute 'points\_to\_pixels'
* 新版numpy不再支持sum(lists,[])操作，改为 from itertools import chain list(chain.from\_iterable(lists))
* Changes
* Changes
* Change
* Change
* Change
* 优化了oncoplot的图例间隔和图例位置定位方法
* 优化timescape，新增进化树EvolutionTree作图
* 新增timescape画图和trtmb计算
* 更新testbox，支持超过两个分组
* Delete sampleid.pdf
* Change
* Change
* Change
* Change
* fix: analysis.cox 在检查重复列时不会再将time和status列入检查范围
* Changes
* Changes
* Update README.md
* Changes
* Changes
* Update README.md
* 新增preprocessing.km\_best\_cutoff, 以p值为导向找到最佳cutoff
* 优化circos图, 添加data package
* cox识别的离散变量类型中添加了bool(最开始只有object)
* Changes
* 修复了一个oncoplot的bug
* kaplan\_meier增加了cox\_table\_loc参数用来设置左上角表格的位置
* analysis.cox分析增加了ref\_dict参数用来指定每个变量的reference plot.forest移除df参数 analysis.cox返回的cox\_result中新增cox\_input属性，对应forest中的被移除的df参数
* Changes
* Changes
